# key/.env.example ← これはサンプルファイルです。実際の設定は key/.env に記述してください。

# SITE_ROOT_URLは公開されるサイトのドメインを指定します（末尾にスラッシュ不要）
# 例: https://example.com
SITE_ROOT_URL=https://example.com

# BASE_PATHはFlaskアプリのURLパスのベースです。
# サブフォルダに配置する場合はスラッシュで囲む（例：/myapp/）
# ルート直下の場合は / を指定してください
BASE_PATH=/myapp

# 環境タイプ（lolipop-prod, lolipop-test, docker）
ENV_TYPE=docker

# ログの詳細レベル（DEBUG, INFO, WARNING, ERRORなど）
LOG_LEVEL=DEBUG

# Flaskのセキュリティキー（CSRF保護やセッション暗号化に必要）
# 実運用では十分に複雑でランダムな文字列を指定してください
SECRET_KEY=replace-with-random-secret-key

# データベース設定（例: docker, lolipop）
DATABASE_URI_DOCKER=mysql+pymysql://docker_user:docker_pass@db:3306/testdb?charset=utf8mb4
DATABASE_URI_LOLIPOP_TEST=mysql+pymysql://username:password@mysql-hostname.lan/dbname-test
DATABASE_URI_LOLIPOP_PROD=mysql+pymysql://username:password@mysql-hostname.lan/dbname-prod

# メール設定
EMAIL_ADDRESS=your-address@example.com
EMAIL_PASS=your-email-password
SMTP_SERVER=smtp.example.com
SMTP_PORT=465

# DBコンテナ用の設定（docker-compose が読む）
MYSQL_ROOT_PASSWORD=example-rootpassword
MYSQL_DATABASE=testdb
MYSQL_USER=docker_user
MYSQL_PASSWORD=docker_pass
